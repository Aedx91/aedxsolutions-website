/// <reference types="@playwright/test" />

// This file ensures the Playwright test types are included globally so that
// fixtures like `page` are strongly typed and the module '@playwright/test'
// resolves under strict + bundler moduleResolution.